# PLAN-002 — APPROVAL

status: APPROVED
actor: consensus (advisor residuals incorporated verbatim in v3; owner mediating live)
date: 2026-07-20

## Registro

Trajeto: PLAN v1 → ADVISOR-REVIEW-1 (DISCORDA, 10 objeções) → CLAUDE-REBUTTAL + PLAN v2 →
ADVISOR-REVIEW-2 (aceita 4; DISCORDA residual em 6, todos PRESCRITIVOS de completude:
tabela de recuperação com linhas de token local e rediagnóstico da linha 3; coreografia
de commits com BASE-SHA pós-descarga + atribuibilidade verificada + guarda de stop
concorrente; matriz control×arquivos completa; sequência canônica com checkpoints
observáveis e `evidence` no payload; schemas JSON exatos por comando + detail tipado +
redaction declarada; lista integral de gates por linha/fronteira) → **PLAN v3 incorpora
os 6 verbatim** — não resta divergência de posição (precedente: PLAN-001).

O dono, mediando ao vivo, mantém o veto natural: o build só inicia com ordem explícita
`/regent build PLAN-002` (REQ-005 §2 — note: há dois planos APPROVED no histórico; o
PLAN-001 já tem build concluído, então o único candidato é o PLAN-002 e o bare
`/regent build` também resolve).

## Plano aprovado

PLAN.md v3 — 5 etapas (errata do lock file + camada de aplicação `activity.py` com tabela
de recuperação de 12 linhas; CLI status/activity/stop com contrato JSON fechado; upgrade
v0→v1 por manifesto; skills v1 control-backed; consolidação 0.4.0 com e2e e testes de
coreografia), ~40 testes dirigidos nomeados, emendas declaradas (REQ-005 §3 coreografia;
errata PLAN-001 lock file no XDG).
